class Plant {
  final int id;
  final String name;
  final String type;
  final String imagePath;
  final DateTime lastWatered;

  Plant(this.id, this.name, this.type, this.imagePath, this.lastWatered);
}